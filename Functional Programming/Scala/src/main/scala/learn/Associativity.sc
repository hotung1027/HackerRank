/** f:(A,A) =>A is ""associative"" <=> for every x,y
  * such that f(x,f(y,z) = f(f(x,y),z)
  *
  * Multiplication of Matrix is associative but not commutative
  *
  * f:(A,A) =>A is ""commutative"" <=> for every x,y
  * such that f(x,y) = f(y,x)
  *
  * 
  */

/**
  *  For example Summation of power of array of integers
  *	:
  *	reduce(map(a,power(abs(_),p)),_+_)
  */

/**
  * Parallelizm is if and only if workable on associative function
  */
